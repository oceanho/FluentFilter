namespace FluentFilter
{
    /// <summary>
    /// This is a enum for Group Filter Options
    /// </summary>
    public enum GroupFilterOption : byte
    {
        Or = 1,
        And = 2,
        Default = Or
    }
}
